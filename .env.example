# Public frontend configuration (no secrets)
# Replace with your real Supabase project reference during setup.
PUBLIC_SUPABASE_FUNCTIONS_BASE_URL=https://xcsptvntvrizwhskaphr.supabase.co/functions/v1

# Server-side sdf-inbound-email configuration (never expose in browser code)
RESEND_WEBHOOK_SECRET=REPLACE_WITH_RESEND_WEBHOOK_SIGNING_SECRET
SDF_INBOUND_RECIPIENT=REPLACE_WITH_CONFIGURED_SDF_INBOUND_RECIPIENT
