# Public URL returned by commercial-operator-command after deployment.
LWS_PREVIEW_HOST_URL=https://preview.lorenzowebsolutions.be

# Must match the Cloudflare Worker secret. Generate only at execution time.
LWS_PREVIEW_ORIGIN_TOKEN=
